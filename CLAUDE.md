# CLAUDE.md — Fakeamon

Context for Claude Code. Read this first every session. The full design lives in **`DESIGN.md`** — that's the source of truth; this file is the working brief.

---

## The project

**Fakeamon** is a turn-based, monster-collecting RPG (Pokémon-style) built as a father-and-son project. You explore an overworld, catch creatures called **Fakeamon**, battle them turn-by-turn, and eventually stop a legendary Fakeamon named **Artemis** from dropping a meteor on the world. Full spec: `DESIGN.md`.

## Who's building this — and how to work with us

- **Jeff** (experienced, drives the repo) and **Lewis** (age 10, co-designer and co-coder).
- This is a **teaching project as much as a coding project.** Please:
  - Explain changes in plain language a 10-year-old can follow. Say *what* changed and *why*, briefly.
  - Prefer **small, readable steps** over clever or dense code. Clear names, short functions, helpful comments.
  - After each change, make sure it **actually runs** so Lewis can see the result right away.
  - Move **one small step at a time.** Suggest the next step; don't sprint ahead five features.
  - Make **numbers easy to tweak** (move power, HP, catch rate). Lewis will want to experiment — keep game values in obvious, well-labeled spots.
  - Keep it **fun and encouraging.** This should feel like play.

## 🤝 Start every session with the Model Huddle

**Before writing any code**, run the Model Huddle from **`MODELS.md`**:

1. Ask **which roadmap step** we're doing.
2. State the **recommended model + effort** from the table in `MODELS.md` and
   ask Jeff & Lewis to **discuss and confirm or override**. If the session's
   current model/effort doesn't match their pick, remind them to run
   `/model` and `/effort` first, then wait.
3. Ask **Lewis for his prediction**: which files will change, and what's the
   riskiest part? (Compare against the actual diff at the end of the step.)

Don't skip this even if the step seems obvious — the huddle *is* part of the
project. After the step works, briefly reflect: was the model pick right?

## Current status

> **Milestone: M5 — Depth & Story. UNDERWAY 🟡 — M5 Step 1 is nearly done: the M5 plan's S5 (XP & levelling) AND S6 (evolution machinery) both landed 2026-08-13. Only S7 (the evolve ceremony) is left to finish M5 Step 1.**
>
> **M5-plan S6 — EVERYTHING EVOLVES NOW (2026-08-13).** The planned one-liner really was one line: **`individual.speciesKey = species.evolvesTo`**. Everything else — bigger stats, a new picture, a new move set — follows for free, because an individual only ever stores *which* species it is (the M5 plan's S1 decision, five milestones ago, finally paying off). `src/progression.js` gained `evolutionFor()` (is it due?), `evolveNow()` (do it), `applyEvolutions()` (walk the party + Boxes) and the **`onEvolve(individual, oldKey, newKey)` hook that S7's ceremony will hang off** — machinery here, showbiz there. It runs from **`evolveWhoeverIsReady()` in `src/main.js`**, after a battle and after a load, **never inside battle resolution** (M5 plan §3 — nobody changes shape mid-turn). Evolving keeps the same **fraction** of HP (half health in, half health out) and **won't revive** a fainted Fakeamon. **Data:** all three starters and all 26 wild Fakeamon now carry `evolvesTo`/`evolvesAt`, and **29 evolved species were sliced and credited** — every one was already inside the verified 198-sheet pool, so nothing new needed a licence check (`CREDITS.md`). ⚠️ **Two things worth knowing.** (1) **The starters' evolutions are a CHOICE, not Tuxemon canon** (`DECISIONS.md` #85): Growler's real evolution (Cobarett) isn't in our art set, and **Whaley and Leafick have no evolution at all** — they're already their families' final forms. Lewis picked "use art we already own", so it's Growler → **Deviraptor**, Whaley → **Leviadile**, Leafick → **Dragarbor** (all dragons, by accident). Each is a one-word `evolvesTo` edit. (2) **Five new moves had to exist first** (`DECISIONS.md` #87) — the reward for evolving is your new form's move kit, but grass had exactly ONE attack in the whole game, so a fully evolved grass Fakeamon fought like a baby one. Firestorm / Tidal Wave / Vine Lash / Forest Fury / Slam fix that; **their names are placeholders awaiting Lewis**, like the 55 creature names. Levels: starters at **16**, wild Fakeamon at **Tuxemon's own numbers compressed to our 30-level game** (#86). Verified with a 23-check S6 suite (data integrity for all 29 lines, the swap, the HP rule, the hook, "the battle never evolves anybody", and an evolved Fakeamon surviving a save), with S5's 31 checks and the 23-check M3/M4 regression pass still green.
>
> **M5-plan S5 — LEVELS ARE REAL NOW (2026-08-13).** The single biggest thing that was waiting in M5: `STAT_GROWTH_PER_LEVEL` in `src/state.js` was **all zeros**, so a creature's `level` changed nothing, anywhere — not for gym teams, not for The Lagoon's higher-level roster. It's now **`{ hp: 3, attack: 2, defense: 1, speed: 1 }`** (`DECISIONS.md` #82), and every difficulty number written over the last four milestones suddenly means something. New file **`src/progression.js`** owns the rules and every tunable number: `xpToNext(level)` = `XP_BASE × level` (10 × L), a level cap of 30, `xpReward()` = `XP_REWARD_BASE × the opponent's level` (8 × L, halved for a catch, ×3 for a mini-boss — the M5S2 socket), and `giveXP()`, the **only** way XP ever goes up. The division of labour is the M5 plan §2's: **`battle.js` reports the win, `progression.js` decides what it's worth** — battle.js just prints the messages that come back ("Growler grew to level 7! ⭐" now appears in the battle log, and the fighter cards show a **Lv** badge). Levelling up is **not** a free heal (current HP rises by exactly the max-HP gain, so half-health stays half-health) and **does not revive** a fainted Fakeamon. Two balance calls came with it, both settled from a **400-fight-per-matchup simulation** rather than a guess: **your starter now begins at level 5** (`DECISIONS.md` #83 — a level-1 starter was losing its opening fights in a level 2–5 meadow), and **The Lagoon dropped from level 10–15 to 8–12** (#84 — Jeff & Lewis's call). ⚠️ **The one thing to know before tuning anything:** because fights are short (~3 hits), **a level gap is decisive** — level-for-level is a comfortable win (~85%), one level down is ~8%, two levels down is ~1%. Levels are a *blunt* lever; base stats are the fine one. **Old saves needed no version bump** but did need care, and Lewis predicted exactly this at the huddle: an M4-era save has Fakeamon sitting on XP that was banked but never spendable, so `parseSave` (`src/save.js`) now runs `catchUpLevels` over the party **and** the box on every load — press Continue and your team cashes in months of XP at once. Verified with a 31-check headless-browser suite plus a 23-check regression pass over M3/M4 (boot, starter, battle, all three building panels, the gym, save round-trip, both maps), and `tools/check-maps.mjs` + `tools/check-roadmap.mjs` both green.
>
> **Milestone: M4 — World Systems. COMPLETE ✅ (2026-08-10) — M4S1–S3 done 2026-07-24, M4S4 2026-07-25, M4S5 2026-07-26, M4S6 2026-08-10 → 31/36 steps total; next up is M5 Step 1 (Evolutions). (M3 fully complete ✅ before it: S1–S4 2026-07-09/10; S5–S7 2026-07-11; S8–S11 2026-07-12.)** Phaser's in, The Meadows renders, you walk it with an animated hero (keyboard or the S10 on-screen D-pad, "Pocket Venta"), wild Fakeamon idle in the grass, walking into one opens a real battle (win/catch clears it — but cleared ones **respawn over time**, S8, so The Meadows never empties out — fleeing leaves it), catching or beating one earns real XP (S8), and the temporary "Battle test" button is gone (S9). **S11 (2026-07-12)** swapped The Meadows' 3-starter placeholder encounters for its real **14-line wild-roster slice** (`VENTA_ROSTER_DRAFT.md`) — `aardorn`/`baoby`/`capiti`/`chenipode`/`chickadee`/`dandicub`/`hatchling`/`lambert`/`marvillar`/`pairagrin`/`pantherafira`/`shybulb`/`snaki`/`tumbleworm`, sprites sliced (`tools/slice-sheets.mjs`, now with an optional slug-filter arg), stats from a `[TUNE]` per-type archetype table in `src/data/fakeamon.js` (not hand-authored — Jeff/Lewis weren't here to tune 14 monsters live). ⚠️ **Names are placeholder Title-Cased slugs — Lewis's rename pass is still pending**, nothing here guesses on his behalf. The roadmap uses the **MxSy** convention (`roadmap.html`/`ROADMAP.md` share one numbering with the plans, `tools/check-roadmap.mjs` keeps counts in sync). M2 is FULLY complete ✅ too (save v1 + export/import). The overworld is the hub — walking into a wild Fakeamon reaches a battle via the `src/screens.js` handoff, then returns to the map. **Offline play added (2026-07-12, Jeff's request):** the game is now a PWA — `manifest.webmanifest` + `service-worker.js` cache everything on first visit, so it plays with no network after that, and "Add to Home Screen" on Android installs a real app icon (placeholder art — Growler — via `tools/make-icon.mjs`). Verified: a fully offline reload still loads the title screen, Continue, walks the map, and fights a real battle. **M4 is now planned** — `PLANS/M4_WORLD_SYSTEMS_PLAN.md` (2026-07-22, from the M4 peer-review checkpoint + a paired code review): it pre-declares the M4 save fields (save v2), adds an enemy-party seam for Gym 1, a `src/data/economy.js` constants file, **re-scopes M4 Step 6** to "open one new area (The Lagoon)" with the rest → M5, and flags small pre-M4 fixes — including a live **High** bug, CR-A (cleared wild Fakeamon reappear + a reload XP-farm after Continue, because the map scene never re-syncs to the loaded save). **M4 is underway (2026-07-24):** M4S1 (Tokens) is done — a win banks `ECONOMY.TOKENS_PER_WILD_WIN` and a 🪙 counter shows it (`src/data/economy.js`). The **S0 groundwork** landed with it: **save v2** (new `tokens`/`flags`/`inventory.berries` fields + a v1→v2 migration and defensive back-fills — `src/state.js`, `src/save.js`), the **CR-A fix** (a scene-resync seam — `WorldScene.rebuildFromState()` in `src/world/config.js`, called from `showWorld()` — so beaten wild Fakeamon no longer reappear after Continue, closing the reload XP-farm), and **CR-C** type colors for normal/metal creatures (`index.html`). Verified in a headless-browser smoke test (boot, token payout, the v1→v2 migration, and the CR-A resync). **M4S2 (Fakeatent) is also done (2026-07-24):** a building tile in The Meadows (`src/data/maps.js`'s `buildings` list) you bump to open a heal panel — 10 🪙 for a full-team heal (Lewis & Jeff's call, `ECONOMY.HEAL_COST`) — via a new `enterBuilding`/`showFakeatentPanel` flow in `src/main.js` and `WorldScene.spawnBuildings`/`buildingAt`/`handleBuilding` in `src/world/config.js`. Unlike a battle, the map stays visible behind the panel (a HUD overlay, per the M4 plan §5.1) — only walking freezes. It also replaces the old M3 team-wipe placeholder: a whole-team faint now sends you to the Fakeatent (`homeBaseTile()`), healed, minus `TEAM_WIPE_TOKEN_LOSS` tokens (floored at zero). Real art landed mid-session — Jeff's own AI-generated pink tent sprite (background fixed in GIMP, verified genuinely transparent, cropped + scaled by Claude) — wired in via a new `BUILDING_ART` lookup that falls back to a colored emoji marker (`BUILDING_LOOKS`) for any building kind without art yet. Verified in a headless-browser smoke test (spawn, bump-to-open, resting, the too-poor path, and the team-wipe path). **M4S3 (Tall Tower) is also done (2026-07-24):** a second building right beside the Fakeatent (same map data, same `enterBuilding` seam) — bump it to open a shop panel and buy Fakeaballs for `ECONOMY.BALL_COST` (5 🪙 each) into the same `inventory.balls.fakeaball` count the catch button reads. Unlike the Fakeatent's Rest, buying **stays in the shop** (re-renders instead of returning to the map) so you can buy several in one visit. Only the basic Fakeaball is sold (Great/Ultra/Cosmic are still a later feature). This session also sourced **real Tuxemon ball-tier art for all 4 tiers** (`assets/sprites/items/fakeaball.png` / `greatfakeaball.png` / `ultrafakeaball.png`, plus a custom AI-generated `cosmicfakeaball.png`) — verified against Tuxemon's `ATTRIBUTIONS.md` (CC BY-SA 4.0, JaskRendix/tamashihoshi) and pulled from the exact commit this project already pins. Only the basic **Fakeaball**'s icon is wired in so far (on the catch button and the shop's Buy button); the other three are vendored + credited in `CREDITS.md` but not used in any screen yet — staged for whenever the tier mechanics get built. *(The Tall Tower shipped with a placeholder marker; **real art landed 2026-07-26** — see the art note at the end of this status.)* Verified in a headless-browser smoke test (both buildings coexist, bump-to-open, buying twice in one visit, the too-poor path, and leaving). **M4S4 (Gym 1) is also done (2026-07-25)** — the milestone's **one real engine change**. `src/battle.js` learned that the *enemy* has a bench too: `config.enemyParty` (an array) + `enemyIndex`, with **`activeOpponent()` sitting beside the existing `activePlayer()`** so both sides of the fight read identically; a lone `config.enemy` is normalised to a bench of one, so **wild battles are behaviourally unchanged** (smoke-tested). All faint handling funnels through one new **`resolveFaint()`** — the single spot that branches "send out the next one" vs "end the battle" — and **XP moved to per-knockout** (a running `xpEarnedThisBattle`, reported at the end), so a 2-mon gym pays twice where `endBattle` used to pay once. A third building joins The Meadows' town row (tile 15,7, same `enterBuilding` seam, **no changes to it**): bump the ⚙️ Gym to meet **Enforcer Boss** and fight **Allagon** then the ace **AV8R** (`src/data/gyms.js` — new). Gyms allow **no running and no catching** (Jeff & Lewis's call, `DECISIONS.md` #62 — B1's "Run always works" was about *wild* battles). Winning pays `GYM_REWARD` (40 🪙) + the **Gear Badge** (a chip in the HUD, `renderBadges()`) and pushes `theLagoon` onto `flags.unlockedAreas`, so **M4S6's gate already has something to open**; a rematch pays `GYM_REMATCH_REWARD` (B17). The game's first metal move, **Iron Beam** (Lewis's invention, `src/data/moves.js`), closes the plan's CR-D gap. **No save-schema change was needed** — `flags.badges/gymsCleared/unlockedAreas` were pre-declared in save v2 (the M4 plan's §2 discipline paying off). *(Both of M4S4's placeholders are now resolved: **AV8R's art landed 2026-07-26** — vendored via a patch from a Cowork session, with its credit recorded as an ⚠️ **assumption** to recheck once `wiki.tuxemon.org` is reachable, since `av8r` has no monster entry in Tuxemon's `ATTRIBUTIONS.md`; and the **Gym building got real art** the same day. Allagon's art was in and fully credited from the start.)* Verified with a 34-check headless-browser smoke test plus a separate loss-path test. ⚠️ **Two things Jeff & Lewis should know:** (1) **a gym mon's `level` changes NO stats** — `STAT_GROWTH_PER_LEVEL` is still all zeros until M5, so Gym 1's difficulty lives entirely in its hand-authored base stats (loudly `[TUNE]`-flagged in `src/data/fakeamon.js`; the same trap waits for every boss stat block written before M5) — ✅ **fixed 2026-08-13 by M5-plan S5**: levels are real now, and Gym 1's Allagon (L8) / AV8R (L10) got a genuine difficulty bump for free; (2) **one faint still ends the whole battle**, even with healthy teammates — a pre-existing M1 rule, now lopsided since the gym leader *does* get a second Fakeamon. Filed as creative homework **B45**, not fixed unilaterally. **Next build step: M4 Step 5 (Cooking Cabin).**
>
> **Playtest round 1 (2026-07-25, after M4S4)** — Jeff & Lewis played and reported six things; all six are fixed (`DECISIONS.md` #66–70). **One real bug:** you could throw a Fakeaball at zero and the counter went to **−1**. Cause: `setControlsEnabled(true)` re-enabled *every* `.move-btn` at the end of a turn, including the catch button `showMoveButtons` had deliberately disabled — so one ordinary attack after your last ball made it live again. Fixed at three layers (`canThrowBall()` is now the single source of truth, `setControlsEnabled` respects it, `attemptCatch` re-checks, and the decrement is floored at 0). **Catching re-tuned:** the old `chance = 0.5 × missingHealth` used 0.5 as a *ceiling*, so a Fakeamon on 1 HP was still a coin flip; it's now `missingHealth²` (`CATCH_CURVE`), which keeps Lewis's B12 anchor (~1 in 4 at half HP) **exactly** while taking 1 HP from ~49% to ~95%. **Economy:** healing is 5 🪙 solo / 10 🪙 with a team (`healCost()`), and a wipe is now the heal price **+5** (`teamWipeCost()` — 10 or 15) instead of a flat 5. **The battle log only shows during battles** (`setLogVisible()` in `src/screens.js`); the messages that mattered outside a fight moved to a one-line **news** note on the overworld card (`noteNews()`/`takeNews()` in `src/main.js`) — without that they'd have silently vanished with the log. **An emptied map now always refills** (`refillEmptyMap()`): the S8 respawn roll only ran *after a battle*, so an empty map had nothing left to trigger one — a dead end that also hit anyone reloading into a cleared map. Verified with a 26-check headless-browser test, plus the original −1 repro now failing to reproduce and M4S4's 34 checks still green.
>
> **Playtest round 2 (2026-07-26) — B45 answered and built.** Jeff & Lewis played the merged build and settled three things (`DECISIONS.md` #71–72). Two were "leave it": **walking behind the Tall Tower and Gym stays** (only the doorway tile is solid — they like it, so the footprint-collision idea is dropped), and the **new catch curve feels right** (a catch at 6/36 HP landed first throw). The third is a real rule change: **(B45) when one of your Fakeamon faints, the next one now joins the battle automatically** — you only lose once *every* Fakeamon has fainted. Before this the battle ended the instant your active one dropped, even with three healthy teammates: an M1 leftover from when you only had one Fakeamon, and plainly lopsided once Enforcer Boss started sending out a second. `resolveFaint()` in `src/battle.js` is still the single place that decides, and now treats both sides identically — `sendOutNextPlayer()` mirrors `sendOutNextEnemy()`. The two paths that used to bypass it (fainting mid-catch and mid-switch) were rerouted through it too. **This makes the code match `DESIGN.md` §6**, which always said "when your **whole team** faints." Verified with a 12-check B45 suite (bench hand-off in wild *and* gym battles, the mid-throw faint path, a solo Fakeamon still losing immediately, and the full-wipe payout), all earlier suites still green.
>
> **M4S5 (Cooking Cabin) is done (2026-07-26).** Berries grow on the overworld and — unlike encounters and buildings, which you *bump* — they **don't block you**: you walk over one and it's yours (the pickup fires in `tryWalk`'s tween `onComplete`, i.e. on arrival). `berrySpots` in `src/data/maps.js` marks six patches in The Meadows, all verified clear of the path, every building footprint, and every wild Fakeamon; **which** berry appears is rolled from per-berry weights in the new `src/data/berries.js` — Fakeaberry 50 / Greenberry 20 / Raspberry 20 / Greatberry 8 / Cosmicberry 2 (they sum to 100, so a weight reads as a percentage). **The Factory grows none at all**; Bossberry has weight 0 and no art, since it only drops from mini-bosses (M5). Empty patches regrow each time you step onto the map (`maybeGrowBerries`), and the map is never left completely bare — same "never leave the player with nothing" rule as `refillEmptyMap`. The **Cooking Cabin** (real log-cabin art, tile 24,7 — *not* the obvious 20,7, which would have covered the wild Fakeamon at 21,6) is the fourth building on the town row and reuses the `enterBuilding` seam untouched for the fourth time: pick two berries, see the dish previewed, cook it, and your **active** Fakeamon eats it — free, no chef (Lewis's B26), and you stay in the kitchen to cook again. All 13 recipes come straight from `DESIGN.md` §9 (`src/data/recipes.js`), keyed on the two berry names sorted alphabetically so order never matters, with any unlisted pair falling back to a Basic Dish so cooking can't fail. **Save:** `world.berries` (spotId → berryKey) is a new NESTED field, so `src/save.js` back-fills it for older saves — the CR-B shallow-merge trap, tested explicitly. No `SAVE_VERSION` bump (same precedent as other `world` additions). ⚠️ **This answered B44 in a way that SUPERSEDES B35** — berries are *not* themed by area after all; every area grows every berry and only the odds differ (`DECISIONS.md` #75, raised twice before building). `AREA_BERRIES` in `src/data/berries.js` keeps per-area lists possible as a pure data edit if Lewis ever wants B35 back. Verified with a 27-check headless-browser suite; the other three suites (34 + 12 + 26) all still green.
>
> **M4S6 (open a new area) is done (2026-08-10) — and with it, M4 is complete. 🎉** The milestone's biggest system: **there are now two maps, and you sail between them.** One method, **`WorldScene.loadMap(mapId)`** (`src/world/config.js`), draws *whichever* area you're standing in — it does the first draw, the after-a-battle re-sync (what used to be `rebuildFromState`, the M4 CR-A fix — the old name is kept as a one-line alias so nothing else had to change), **and** the change of area. That's the whole seam: everything that changes what the overworld shows goes through it. Travel is by **boat** (Jeff & Lewis's call, `DECISIONS.md` #76 — "the lagoon has some water theming, the boat seems like a logical way to travel"), tied up at the east end of The Meadows' path and **bumped like a building** rather than walked onto, which is what stops a landing tile from bouncing you straight back. It wears a 🔒 until you've won the **Gear Badge** and a 🚤 after. ⚠️ **The gate reads exactly one list — `flags.unlockedAreas`** (`DECISIONS.md` #79). The M4 plan §5.3 sketched a `requiresBadge` field on the exit, but that list was already pre-declared in save v2 and already written by `awardGymPrize` at M4S4; two records of one fact is how they drift apart, so the exit carries none and the "which badge?" wording on a locked boat is looked up from `GYMS`. **The Lagoon** is a new 30×20 map (`src/data/maps.js`) with **its own tileset** — `assets/tilesets/lagoon.png`, composed by the new **`tools/make-lagoon-tileset.mjs`** from the same George tiles The Meadows uses; unlike `meadow.png`, whose recipe nobody wrote down, that script's RECIPE list *is* the documentation and re-running it reproduces the file. Water is **solid**: you walk around the lagoon, you don't swim — which needed **per-map `solidTiles`**, since tile 13 is a footpath in The Meadows and open water in The Lagoon. It's stocked with its real **12-line wild slice** (`VENTA_ROSTER_DRAFT.md` — `axolightl`/`claymorior`/`fluoresfin`/`gupphish`/`jelillow`/`kroki`/`lesmagu`/`nebufin`/`nostray`/`nudiflot_female`/`nudiflot_male`/`skwib`, sliced + credited), its own **Fakeatent** (so a team-wipe out there heals you *there* — `DECISIONS.md` #77) and its own **berry patches**; no shop/gym/cabin yet, each of which is one map-data line whenever they want it. **No save-version bump was needed** — `world.mapId` has been saved since M3, and encounter ids were already namespaced, so one flat `defeatedEncounters` list still works (`DECISIONS.md` #78); what *did* need fixing is that respawning must be scoped to the map you're on, or a roll in The Lagoon could pick a Meadows creature and silently do nothing (`clearedOnThisMap()` in `src/main.js`). New guard: **`tools/check-maps.mjs`** reads the real `src/data/maps.js` and proves, for every map, that nothing stands inside a tree, nothing shares a tile, everything can be reached on foot, every 2×2 tree is whole, and every exit lands somewhere walkable — run it after touching map data, the same way `check-roadmap.mjs` is run after touching the roadmap. **Three design tweaks landed the same day, straight off the first screenshots (`DECISIONS.md` #80):** (a) **The Lagoon is DARK — dark blue and black.** That's not different tiles, it's the same George tiles put through a `MOOD` pass at the end of `tools/make-lagoon-tileset.mjs`, so gloominess stays a **dial** rather than hand-painted art. The rule is *"the bluer a pixel already is, the more light it keeps"* — which sinks the banks to near-black and leaves the water glowing, instead of the naive "darken everything" that made the water the darkest thing on screen. (b) **The Meadows now has a small inlet of water beside the boat**, so the dock reads as a dock. That needed nine new water tiles in `meadow.png` — added by the new `tools/add-meadow-water-tiles.mjs`, which **grows the image by one row (6×4 → 6×5) so no existing tile number moves**; tiles 21–29 are the water, and it's safe to re-run. (c) **The Lagoon's boat sits on the map's far WEST edge**, mirroring The Meadows' dock on its far east — sail east off one map, arrive at the west of the next. Exit markers also got `EXIT_DEPTH`, so the hero walks *in front* of a moored boat instead of vanishing behind its marker. Verified with a 56-check headless-browser suite plus a 19-check regression pass over M3 and M4S2–S5. ⚠️ **Two things Jeff & Lewis should know:** (1) **Lewis's rename pass is still pending** for these 12, same as The Meadows' 14 — the names are Title-Cased slugs; (2) **ten of the twelve are water type, so they share identical archetype stats and moves** — only art and name differ, and the area's wild levels (10–15 vs The Meadows' 2–5) still change nothing until M5 adds leveling. Picking a few to hand-tune is good creative homework once they've played it.
>
> **Playtest round 3 (2026-08-11, after M4S6 shipped) — three bugs, two root causes.** Jeff & Lewis played the merged build and reported: berries you could walk over but not pick up; berries sitting somewhere unreachable; and being able to walk through the Cooking Cabin without its panel opening. **The first two were one bug.** `spawnBerries()` reassigned `this.berrySprites = []` **without destroying the sprites already on screen** — emptying our list doesn't remove anything from the screen, it just loses the handle. `loadMap()` destroyed them first so it looked fine, but `maybeGrowBerries()` (`src/main.js`) calls `spawnBerries` **directly**, so every berry that grew orphaned the entire previous set: untouchable ghost berries, and — after sailing — The Meadows' berries still painted over The Lagoon at Meadows coordinates, where the same tile can be open water. Reproduced before fixing (13 drawn vs 5 tracked after six trips to the map). The fix is at the root and removes the whole trap class: **all four `spawn*` methods now clear their own sprites first** (`clearSprites()`), instead of trusting the caller to have done it — so `loadMap` no longer has a manual destroy block to keep in step. **The third bug is separate:** a building is pinned to ONE tile but its picture is up to three tiles wide, so the Cabin's left and right thirds did nothing. Now a building blocks and opens **every tile along the bottom of its picture** (`WorldScene.buildingFootprint`, measured off the sprite's own width so no number needs keeping in sync). ⚠️ This **amends `DECISIONS.md` #71 rather than cancelling it** — the tall parts are still walk-behind, so strolling behind the Tall Tower and the Gym works exactly as Jeff & Lewis wanted; you just can't walk through a front door any more. `tools/check-maps.mjs` learned the same footprint rule (it reads the sprite PNGs) so the guard can't under-report reachability. Verified with a 27-check regression suite — *checked against the un-fixed code first, where the new checks correctly fail* — plus M4S6's 56 checks still green.
>
> **B34 built (2026-07-26) — a decided-but-never-shipped item, found in a pending-work sweep.** Lewis decided on **2026-07-11** (`DECISIONS.md` #52) that the wild opponent should read **"the wild `<name>`"** everywhere it's mentioned, precisely so a mirror match is readable without nicknames — and it was logged ✅ but never actually built. A Growler vs a wild Growler still printed two identical "Growler used …" lines. Now one **`fighterName(role)`** helper in `src/battle.js` decides what to call each side (your own keeps its plain name), used by the log, the result messages, the fighter-card heading, and the battle title. ⚠️ **One extension:** B34 predates gyms, so "the wild Allagon" would be wrong for a leader's Fakeamon — a trainer battle reads **"Enforcer Boss's Allagon"** instead. Verified with a 16-check suite that also asserts "the wild" never leaks into a gym battle. **Process note for future sessions: a ✅ in the DECISIONS table means *decided*, not *shipped*.**
> M1 (the battle slice) is complete and live. M2's steps: code split into `src/` files (Step 1), Leafick + a Choose Your Starter screen (Step 2), a random wild opponent every battle via the `startBattle(config)` contract with a Run button that always works (Step 3), a **Throw Fakeaball** catch action using the 50%-base capture formula (Step 4), the M5-plan **S1** state foundation (species keyed in `FAKEAMON`, `src/state.js`'s `newIndividual()`/`statsFor()`, every fighter an individual with its own `currentHP`), **Step 5**'s **team list** (`gameState.party`/`gameState.box`, up to 4 active + Boxes overflow, no nicknames — Lewis's B3), and now **Step 6** (2026-07-08): **Switch** works two ways — mid-battle, it's a real turn-costing action next to Attack/Catch/Run (same speed-order risk as Catch: faster switches in safely, slower risks your current fighter fainting before the swap completes — Jeff & Lewis's live call, decision #48), and between encounters it's free from the team screen. Mid-battle switching required upgrading `battle.js` to the M3 plan's full contract shape (`config.playerParty` — a live reference to `gameState.party` — instead of a single `config.player`), so `party[0]` is always "who's fighting" and a switch is instantly visible everywhere via a new `onStateChange` hook that keeps the team row in sync live. **S3 (save v1) and S4 (export/import) are now done (2026-07-10, `src/save.js`), so M2 is fully closed out; M3 is fully done too (see the milestone line above). Next up:** **M4** Step 1 (Tokens). Lewis cleared the homework backlog on 2026-07-06 (B1–B32) and answered the last ten (B33–B42) on 2026-07-11 — every creative-director question B1–B42 is now decided (`DECISIONS.md` rows 14–45 and 51–60); only Jeff's number-tuning list remains. Live at [jhester599.github.io/fakeamon_spark](https://jhester599.github.io/fakeamon_spark/), auto-deployed via GitHub Pages.
>
> **Two small M2 follow-ups (decided 2026-07-10, built 2026-07-22):** Fakeaballs are now limited — `gameState.inventory.balls.fakeaball` starts at **5** (`STARTING_FAKEABALLS`, `src/state.js`), each throw decrements it (caught or not), and the "Throw Fakeaball" button shows the live count and disables at zero. And a successful catch now pauses on a **Continue** button (`showContinueButton`, `src/battle.js`) the same way a win/loss does, instead of resolving straight into the next screen. Both wired through the `config.inventory` live reference (same pattern as `config.playerParty`) and persisted in save v1 (`src/save.js`, no version bump needed — same trick as `world`). See `DECISIONS.md` #49–50, `DESIGN.md` §6/§9.
>
> **M3 prep is staged ("M3S0", 2026-07-06):** meadow tilesets + hero walk sheet + Frondly vendored (Leafick has real art now — all three starters do), the sprite-slicer tool built and run, `src/data/maps.js` drafted with The Meadows, and Phaser 4 skills vendored. Full inventory: `PLANS/M3_OVERWORLD_PLAN.md` **§A.4** — read §A before any M3 session. **M3 *code* steps S1–S4 are done ✅ (2026-07-09/10): Phaser 4.2.1 vendored to `assets/vendor/`; `src/world/config.js` draws The Meadows from `src/data/maps.js` and walks an animated hero on the grid (tween movement, tree/edge collision, position saved); the overworld is the hub and the temporary "Battle test" button bridges to a battle until the S7 handoff. See `PLANS/M3_OVERWORLD_PLAN.md` §A.5 (S1) + §A.6 (S2–S4). **S5–S7 done 2026-07-11** — wild Fakeamon idle on the map (`spawnEncounters`), and walking into one opens the real battle via the new `src/screens.js` screen manager + the `handleEncounter`→`startMapEncounter`→`enterBattle` handoff (win/catch `removeEncounter`s it, tracked in `defeatedEncounters`). **S8 done 2026-07-12** — real XP + wild-Fakeamon respawn (see the milestone line above). **S9 done 2026-07-12** — the temporary "Battle test" button and its dead code are gone. **S10 done 2026-07-12** — touch/mobile play, see the milestone line above. Next: S11.** **Wild-roster art is FULLY staged (2026-07-06, Jeff's Cowork wiki run):** the entire §16 pool — now **198** after `bearloch`/`foxko` were dropped for having no credit anywhere — is vendored with verified attribution (**198 staged, 0 pending** in `CREDITS_ROSTER.md`), reference data in `tools/roster-200.json`. Two ⚠️ remain before those specific sprites reach the live game: the OPMon-derived trio (coaldiak/ninjasmine/toxiris — confirm OPMon's terms) and the hero sheet's Catch Challenger share-alike confirmation (`CREDITS.md`). **Every wild Fakeamon also has a home area** (`VENTA_ROSTER_DRAFT.md` + `areaProposed` in `tools/roster-200.json`) — Lewis's homework **B37**, now **approved as-is (2026-07-11, `DECISIONS.md` #55)**: `areaProposed` becomes the real `area` as each area's roster is wired in (M3S11 → M4S6).

## Scope guardrails

**Build now (M5 — depth & story):** M1–M4 are all complete, and **M5 Step 1 is underway** — read **`PLANS/M5_STATE_AND_SAVE_PLAN.md` §A first**; it owns game state, XP, evolution and saving, and reconciles the plans against the code as it actually exists. ✅ **Its S5 (XP & levelling) and S6 (evolution machinery) are both DONE (2026-08-13)** — see the status lines above. **Next is S7, the evolve ceremony**, which finishes M5 Step 1: Lewis's **B23** pick (`DECISIONS.md` #36) is a *full* ceremony — screen flash, "What?! ‹name› is evolving!", big sprite reveal. Everything it needs is already waiting: implement the **`onEvolve(individual, oldKey, newKey)` hook** in `src/progression.js` (currently an empty function, deliberately) and play the show on the map, where the reveal reads best. **Don't put the ceremony in `progression.js` itself** — the split is the point: rules there, theatre elsewhere, so Lewis can redirect the show without touching the rules.

Build against the settled seams: `startBattle(config)` (`src/battle.js` — takes **either** `enemy` for a wild fight **or** `enemyParty` + `trainerName` for a trainer one), the screen manager (`src/screens.js`), the save bag (`src/state.js`/`src/save.js`), the building bump seam (`enterBuilding` in `src/main.js` + `spawnBuildings`/`buildingAt`/`handleBuilding` in `src/world/config.js` — a new building kind is just a map-data entry + a `BUILDING_LOOKS` fallback + one dispatcher branch; proved four times now), the matching **exit** seam for travel between areas (`enterExit` + `spawnExits`/`exitAt`/`handleExit`), and **`WorldScene.loadMap(mapId)`** — the one place a map gets drawn. **Adding a new area is now a data job:** an entry in `src/data/maps.js` (30×20, its own tileset + `solidTiles`), an `exits` boat at each end, the destination pushed to `flags.unlockedAreas` by whatever unlocks it, its wild slice sliced + credited, and `node tools/check-maps.mjs` green.

**Do NOT build yet:** mini-bosses, Artemis, the box-swap UI (M5-plan S8), the Great/Ultra/Cosmic Fakeaball **mechanics** (catch-rate bonuses, separate inventory counts, shop pricing — their *art* is vendored/staged, per-tier gameplay is not), day/night, and gym leaders 2–3. The **other four areas** (The Forest, Foggy City, Snow Mountain, The Factory) are M5 work, taken **one at a time** as their paths open — don't do all four in a session. Keep to the M5 plan's step order rather than front-running it. *(M1–M3 are the historical record — the battle slice, then catching + a team of 4 with Boxes + switching + save, then the Phaser overworld; full detail in `ROADMAP.md`.)*

**Scheduled but paced area-by-area — check the step before touching:** expanding the wild-encounter roster beyond the starters. `CONTENT_REFERENCE.md` §16 has ~200 Tuxemon (198 staged) pre-selected and typed for this. It's on the roadmap, split by area: **M3S11** wires in The Meadows' slice (the one map M3 builds), and **M4S6** grows the roster area-by-area as gym badges open each new area (The Forest, Foggy City, Snow Mountain, The Factory, The Lagoon — `DESIGN.md` §7, `DECISIONS.md` B8), carrying on into M5's late-game areas. Don't front-run it: only wire in the slice for the area actually being built. *(The art/licensing legwork for the whole pool IS pre-staged — §16's staging note — the per-area work is the creative wiring: Lewis's renames, encounter tables, stats.)*

## Tech stack

**CONFIRMED (2026-07-06) by Jeff:**

- **M1–M2 (battle, catching, team): plain HTML + CSS + JavaScript, no build step.** A single page: buttons for moves, `<div>` HP bars, a text log. Open the file in a browser and it just works. This keeps things maximally understandable for Lewis — no game-engine concepts needed yet.
- **M3 (overworld) onward:** adopt **Phaser** when we need tile maps and sprite movement. **As of M3 S1 (2026-07-09):** Phaser **4.2.1** is **vendored** at `assets/vendor/phaser.min.js` (MIT) and loaded as a plain `<script>` global — *not* a CDN — so double-click-to-play still works offline (`DECISIONS.md`; `PLANS/M3_OVERWORLD_PLAN.md` §2). ⚠️ Read the matching file in `PLANS/phaser-skills/` before writing any Phaser code.
- **Art:** M1 used placeholder colored boxes + names to start; real Tuxemon sprites are already dropping in for the starters (see Assets below), full roster art lands at M3.

## Current project structure

```
index.html         → loads the game scripts (plain <script> tags, no build step, no modules); hosts #world (Phaser canvas) above #battle (the plain-HTML fight)
assets/vendor/phaser.min.js → the Phaser game engine, vendored & pinned to 4.2.1 (MIT) — see CREDITS.md; loaded before src/world/config.js
src/data/moves.js       → MOVES data (tweak power/accuracy here)
src/data/fakeamon.js    → GROWLER, WHALEY, LEAFICK data (stats, sprite paths)
src/data/typechart.js   → TYPE_CHART matchup table
src/data/economy.js     → ECONOMY constants — token earn/cost numbers (M4S1); [TUNE] every value here
src/data/berries.js     → BERRIES (M4S5) — the six berries, each with a [TUNE] `weight` for how often it's found (they sum to 100, so a weight IS a percentage). AREA_BERRIES is the per-area exception table — The Factory grows none; any area not listed grows everything. randomBerryFor() does the weighted roll
src/data/recipes.js     → RECIPES (M4S5) — DESIGN.md §9's 13 berry pairs → dish + heal amount, keyed on the two berry names sorted alphabetically so order never matters. Anything unlisted makes BASIC_DISH
src/data/gyms.js        → GYMS data (M4S4) — leader, type, badge, and the 2-Fakeamon team per gym. Only gym1 (Enforcer Boss, Metal, Gear Badge) exists; adding Gym 2/3 is another entry here + a building on a map. ✅ a team member's `level` is REAL as of M5 Step 1 — it's a blunt but genuine difficulty dial now (base stats in fakeamon.js are the fine one)
src/data/maps.js        → EVERY area as plain tile arrays + encounters + buildings + berrySpots + exits. Two so far: The Meadows and, since M4S6, The Lagoon. Each map carries its own tileset, its own `solidTiles` list (tile 13 is a footpath in one and open water in the other!) and its own tile legend. Loaded by index.html and drawn by src/world/config.js. Edit a number, refresh, the world changes. Encounters/buildings/exits are all "bump into it" tiles; berries are the odd one out — you walk right over them. ⚠️ Every map must be 30×20; run `node tools/check-maps.mjs` after editing
src/progression.js  → GROWING UP (M5 Step 1 / the M5 plan's S5 + S6) — the XP curve, what a win pays, the level-up loop, AND evolution: evolutionFor() / evolveNow() / applyEvolutions(), plus the onEvolve(individual, oldKey, newKey) hook the S7 ceremony plugs into (empty on purpose — rules live here, theatre does not). Evolving is literally one line, individual.speciesKey = evolvesTo; everything else follows because stats/art/moves all derive from species + level. Called from main.js's evolveWhoeverIsReady(), never from inside a battle. ⭐ giveXP() is the ONLY way XP ever goes up, and every tunable progression number is at the top of this one file (XP_BASE, MAX_LEVEL, XP_REWARD_BASE, CATCH_XP_FRACTION, MINIBOSS_XP_MULT). Battles never do level maths themselves — they call giveXP() and print the messages it hands back. catchUpLevels() is the same loop with no new XP, used by src/save.js to cash in XP banked before levelling existed. ⚠️ How much a level is WORTH lives in STAT_GROWTH_PER_LEVEL in src/state.js, not here
src/battle.js       → the startBattle(config) contract + all battle logic (turns, damage, HP, win/lose/flee). M4S4: BOTH sides have a bench — activePlayer() is party[0], activeOpponent() is enemyParty[enemyIndex]; resolveFaint() is the one place that decides "send out the next one" vs "end the battle" 
src/save.js         → save v2 (M4; was v1 at M5-plan S3/S4): saveGame/loadGame/clearSave + version/migration (v1→v2 adds tokens/flags/inventory.berries), and exportSave/importSaveFromFile. localStorage single slot "fakeamon-save"
src/world/config.js → the Phaser overworld — BootScene (M4S6: preloads EVERY map's art up front, since you can sail at any moment), WorldScene, and startWorld() (Scale Manager FIT mode as of S10). ⭐ **loadMap(mapId) is THE seam**: it draws whichever area you're in — first draw, after-a-battle re-sync (the M4 CR-A fix) and change-of-area, all one method; `rebuildFromState()` is now just an alias for it, so screens.js didn't change. Also walks the hero via heldDirection() (keyboard or the S10 D-pad) with per-map solid-tile collision, and stands the three kinds of "bump into it" tile: wild Fakeamon (spawnEncounters → handleEncounter → the S7 battle handoff / removeEncounter / respawnEncounter), buildings (spawnBuildings → handleBuilding; buildingFootprint makes the whole base row of a sprite solid) and boats (spawnExits → handleExit). ⚠️ Every spawn* clears its own old sprites via clearSprites() — emptying the list without destroying leaves ghosts on screen (the 2026-08-11 berry bug). World numbers (size, zoom cap, color) are labeled constants here
src/world/dpad.js  → the on-screen D-pad, "Pocket Venta" (M3 S10): plain DOM buttons + Pointer Events feed WorldScene's virtualPad seam; PLANS/M3_TOUCH_AND_MOBILE_PLAN.md §3–§6
src/screens.js      → the tiny screen manager (M3 S7): showWorld()/showBattle() swap between the map and the battle — hiding+freezing the map (and the D-pad, S10) during a fight, refocusing it after. The ONLY place #world's visibility + the world scene's paused/input state are touched (M3 plan §3)
src/main.js         → the conductor: title screen (Continue/New Game + Export/Import), boots the overworld (startWorld), runs starter-select, and owns the battle handoff — enterBattle / startMapEncounter / handleBattleOutcome (bump → battle → back to the map), autosaving after every change
roadmap.html        → visual quest map — mirrors ROADMAP.md (one row per MxSy step); update BOTH in the same commit whenever a step's done-status changes, then run `node tools/check-roadmap.mjs` to prove they still agree (see ROADMAP.md golden rule #6), also served live
homework.html        → interactive worksheet for Lewis & Jeff (mirrors HOMEWORK.md)
/assets/sprites      → real Tuxemon-based art in use (growler.png, whaley.png, leafick.png)
/assets/sprites/battle → vendored original source sheets (slicer input)
/assets/sprites/front|back|idle → sliced battle + overworld sprites, made by tools/slice-sheets.mjs (for M3)
/assets/sprites/player → hero.png walk sheet for M3 (⚠️ license check pending — see CREDITS.md)
/assets/sprites/buildings → map building sprites: fakeatent.png (M4S2), talltower.png + gym.png + cabin.png (all 2026-07-26; the Cooking Cabin's art is staged ahead of M4S5 — not on any map yet) — all three are Jeff & Lewis's own AI-generated art, trimmed + scaled to the tile grid. BUILDING_LOOKS in src/world/config.js still holds an emoji fallback per kind, used by any building with no art yet (the Cooking Cabin, M4S5, is next)
/assets/sprites/items → Fakeaball-tier icons (M4S3): fakeaball.png (live, real Tuxemon art) + greatfakeaball.png/ultrafakeaball.png/cosmicfakeaball.png (vendored, staged — not wired into any screen yet)
/assets/sprites/items/berries → berry art, staged for M4S5 (added 2026-07-26, Jeff & Lewis's own): <slug>.png is the 32×32 icon for the Cooking Cabin; pickup/<slug>.png is the 16×16 one that lies on a map tile. Five of the six exist — Bossberry has no art because it only drops from mini-bosses (M5). Not wired into any screen yet
/assets/tilesets     → meadow.png (the composed tileset The Meadows draws with) + the George originals it came from
/assets/icons        → PWA app icons (icon-192.png, icon-512.png), made by tools/make-icon.mjs — placeholder art (Growler's front sprite), swap freely
manifest.webmanifest → PWA manifest (name, icons, standalone display) — what makes "Add to Home Screen" on Android install a real app icon
service-worker.js    → offline play (added 2026-07-12): caches every file the game needs on first (online) visit, so it keeps working with no network after. ⚠️ Read its header comment — CACHE_VERSION must be bumped on every asset-changing deploy, or updates never reach offline players. Registered from src/main.js
sw-precache-list.js  → GENERATED by tools/generate-sw-manifest.mjs — the exact file list service-worker.js caches. Re-run that script (and commit the result) after adding new assets
tools/               → dev-only Node scripts (slice-sheets.mjs + its manifest — takes an optional slug-filter arg; generate-sw-manifest.mjs — regenerates sw-precache-list.js; make-icon.mjs — composes a PWA icon from any sprite; make-lagoon-tileset.mjs — builds assets/tilesets/lagoon.png from George's tiles, and its RECIPE list documents exactly which ones; check-roadmap.mjs — the roadmap consistency guard, run after any step change; check-maps.mjs — the MAP guard, run after editing src/data/maps.js) — never loaded by the game
PLANS/phaser-skills/ → vendored Phaser 4 skill docs — read the matching one BEFORE writing any Phaser code (M3 plan §2)
.github/workflows    → deploy-pages.yml (auto-publishes to GitHub Pages on push to main)

DESIGN.md    → full design spec (source of truth)
CLAUDE.md    → this file — working brief + current milestone
ROADMAP.md   → stepwise M1–M5 build plan, what's done/next
DECISIONS.md → decision log + the loop for folding Lewis's picks into DESIGN.md
MODELS.md    → which Claude model to use per step + the pre-step Model Huddle
PLANS/       → architecture plans from high-end planning sessions:
               M3_OVERWORLD_PLAN.md governs all M3 work AND constrains the
               remaining M2 steps (its §5 battle contract — see the M5
               plan's §A for how); M4_WORLD_SYSTEMS_PLAN.md governs all M4
               work (tokens, buildings, Gym 1, cooking, the area-travel
               seam) — read its §A first, it triages the M4 peer-review +
               a paired code pass against the real code, and lists the small
               pre-M4 fixes (incl. the live CR-A bug); M5_STATE_AND_SAVE_PLAN.md
               owns game state, XP, evolution, and saving — read its §A first,
               it reconciles the plans against the code as it actually exists
HOMEWORK.md  → open questions waiting on Lewis and Jeff
HOMEWORK_BACKLOG.md → the big question bank (B1–B42, all decided) for the whole game;
               HOMEWORK.md gets served 2–4 questions at a time from here
CREDITS.md   → art attribution: file, source, artist, license, commit pulled from
CONTENT_REFERENCE.md → where Tuxemon-sourced art/data/roster ideas come from + licensing rules
README.md    → repo front page, links to the live site
```

**Why plain `<script src="...">` and not ES modules (`import`/`export`)?**
Modules require a server — they fail with a CORS error when you just
double-click `index.html`. Plain scripts sharing the global scope keep
"open the file and it just works" true. Revisit this if the project ever
needs real modules (e.g. bundling for Phaser at M3).

## How to run

**Play it live:** [jhester599.github.io/fakeamon_spark](https://jhester599.github.io/fakeamon_spark/)
— auto-deployed from `main` on every push, always current.

**Or run it locally:** no tooling needed — open `index.html` in a browser,
or use a simple local server like `python3 -m http.server`.

---

## Milestone 2 — build this

Turn the one-off battle into "catch creatures and build a team." **All of
this is now done ✅ (M2 complete, 2026-07-10)** — kept here as the record
(full tables: `ROADMAP.md` M2 + `PLANS/M5_STATE_AND_SAVE_PLAN.md` §6):

| Step | What we build | Where it's specced |
|---|---|---|
| **3** ✅ | A **random wild opponent** — the global `startBattle(config) → Promise<outcome>` contract lives in `src/battle.js`; starter-select moved to `src/main.js`. Flee (Run button) always works (B1); wild level uses the team-average as an M2 stand-in until M3's areas exist (B4, `DESIGN.md` §5). *(Note: this M2-sized contract uses plain `player`/`enemy` species objects, not yet `playerParty`/species-key+level — those arrive with individuals at M5-plan S1 and leveling at M5-plan S5.)* | contract shape: M3 plan §5 · globals version: M5 plan §A.2 |
| **4** ✅ | **Catch action + Fakeaball** — "Throw Fakeaball" button, capture formula (50% base rate × missing-HP fraction, floor/cap; Lewis's call), only the basic ball for now (Great/Ultra/Cosmic bonuses are Jeff's number-tuning). Throwing a ball takes your turn, same speed-order rule as attacking — a failed throw lets the wild Fakeamon counter-attack. Caught Fakeamon are logged as fully healed (B2) with `"Gotcha! <name> was caught!"` / `"Oh no! <name> broke free!"` (B5); the encounter just ends for now — actually joining a team lands with the state bag at Step 5 | formula: `DESIGN.md` §6 |
| *M5-plan **S1** — before Step 5* | **Individuals & the state bag** — `src/state.js`, `newIndividual()`, `statsFor()`; retires the `hp[name]` map so two Growlers don't share HP | M5 plan §1 + §A.3 |
| **5** ✅ | **Team list** — up to 4 active, overflow to Boxes. No nicknames — species names only (B3, decided) | decision #11 |
| **6** ✅ | **Switch** which Fakeamon fights, including from Boxes | — |
| *M5-plan **S3–S4** — end of M2* ✅ | **Save v1** (autosave to localStorage, Continue/New Game title screen) + **export/import** — `src/save.js` | M5 plan §4 |

Battle rules (damage, accuracy, type chart, stats) are unchanged from M1 —
they live in `src/battle.js` and `src/data/*.js` now; those files are the
truth, tweak numbers there. The M1 spec remains in `DESIGN.md` §6 and
`ROADMAP.md`.

**Definition of done for M2:** you can fight a wild Fakeamon, weaken it,
catch it, see it join a team of up to 4 (overflow to Boxes), and swap
fighters — and the battle module is the clean `startBattle` contract that
M3 will call without changes.

---

## Core mechanics reference

Don't re-invent these — they're specified in `DESIGN.md`:
- Battle system, damage & capture formulas → §6
- Types, the Tuxemon→our-type mapping, and the **Metal**/**Cosmic** type decision → §4
- Stats, mini-boss tier, and **Artemis** (HP = 2× strongest mini-boss; self-hitting Meteor Shower) → §5–6
- Full roster: 5 mini-bosses, 3 gyms → §8
- Roadmap M1–M5 → §11

## Data conventions

Keep game content as **plain data**, separate from logic, so it's easy to read and tweak:

```js
// src/data/moves.js
const MOVES = {
  flare: { name: "Flare", type: "fire", power: 18, accuracy: 85 },
  // …
};

// src/data/fakeamon.js
const GROWLER = {
  name: "Growler", type: "fire",
  maxHP: 40, attack: 13, defense: 10, speed: 12,
  moves: ["tackle", "bite", "burn", "flare"],
};
```

No `export`/`import` — see "Why plain `<script src="...">`" above for why.

Type names lowercase and consistent (`"fire"`, `"water"`, `"grass"`, `"metal"`, `"normal"`, `"cosmic"`).

## Assets & attribution

- **Read `CONTENT_REFERENCE.md` before pulling any new Tuxemon art, sound, or creature data** — it's the full guide: why Tuxemon (not Pokémon Void), the complete repo map, per-milestone pull lists, licensing rules, and a 200-monster wild-encounter roster for M3+. This section is the short version.
- Starting art is **Tuxemon** sprites. License is **mixed per asset** (mostly CC BY-SA 4.0, some CC BY 3.0/4.0, a couple Public Domain) — **check each file's actual license before using it**, don't assume one blanket license. Usable with credit; keep art edits under the same (share-alike) license where that applies.
- **Verified source path:** `mods/tuxemon/gfx/sprites/battle/<slug>-sheet.png` on the `development` branch of `github.com/Tuxemon/Tuxemon` — not the wiki, not a guess. Full details and per-creature file list are in `DESIGN.md` §12.
- These are **composite sprite sheets (128×88px)**, not flat single sprites — a 64×64 front pose at (0,0), a 64×64 back pose at (64,0), and a small 2-frame idle animation (two 24×24 frames) at (0,64)/(24,64). *(Corrected 2026-07-06 by measurement — an older "2×2 grid of ~64×44 cells" description was wrong.)* `tools/slice-sheets.mjs` cuts them; scale with nearest-neighbor only.
- Gym-leader (trainer NPC) art lives in a *different* folder: `mods/tuxemon/sprites/<name>.png`.
- When we add any asset, record it in **`CREDITS.md`**: file, our name for it, source path, artist(s), license, and the commit the file was pulled from (the branch moves).
- **M1's gameplay never needed art** — but as a bonus, ahead of schedule, all 3 starters (Growler, Whaley, Leafick) now show real Tuxemon-based art (see `CREDITS.md`). The rest of the roster's art comes later (the slicer is **M3S5**, done; the wild-roster expansion is **M3S11** → **M4S6**, see `ROADMAP.md`) — this section documents what we already confirmed so those steps are fast when we get there.
- **✅ Resolved:** Growler's (Hissiorite) and Leafick's (Frondly) sprites have no entry in Tuxemon's own `ATTRIBUTIONS.md`, but both artists are now confirmed via the wiki — see `CONTENT_REFERENCE.md` §13 and `CREDITS.md`. No longer a blocker for public distribution.

## Git

- Commit **often**, after each small working step, with plain messages ("add damage formula", "growler can now use Flare").
- Keep `main` runnable. Nothing fancy needed — small frequent commits beat big ones.
- **Ticking a step done (or adding/moving/removing one)? Update `ROADMAP.md` and `roadmap.html` together, same commit, then run `node tools/check-roadmap.mjs` — it must print `✅ PASS` before you commit.** The checker recomputes every count from the actual done-marks (`class="done"` / `✅`), checks the meters, and fails if the two files disagree or a counter is wrong — so the roadmap you and Lewis steer by stays accurate. (The header counter drifted and shipped as a real bug **three times** before this guard existed.) Steps are named **`MxSy`** (M‹milestone›S‹step›) — one roadmap row per step. See ROADMAP.md golden rule #6.

## Design decision loop (Lewis is creative director)

Open creative questions live as homework for **Lewis** in `HOMEWORK.md` (writable) and `homework.html` (interactive worksheet). When Jeff relays Lewis's picks, **run the loop** — full details and the question→section index are in **`DECISIONS.md`**:

1. **Log** each pick in the *Decisions Made* table in `DECISIONS.md` (number, decision, Lewis's reason, date), and flip that row's Status to ✅.
2. **Update `DESIGN.md`** — use the Question Index in `DECISIONS.md` to find the matching **[TO DECIDE]** and replace it with the decision, tagged **`DECIDED (date):`** so the open question is gone.
3. **Tick** the question in `HOMEWORK.md` (mark it done).
4. **Update the milestone status** in this file if the decision affects the current build.
5. **Commit** with a plain message, e.g. `"decide: gym badges open a new area"`.

If a pick is unclear or clashes with an existing decision, **ask before writing it in** — don't guess on Lewis's behalf.

## When in doubt

If a design detail isn't decided (e.g., capture tuning, encounter style, whether gyms are single-type), it's marked **[TO DECIDE]** in `DESIGN.md` — **ask Jeff and Lewis rather than guessing.** Small, reversible defaults are fine for prototyping; call them out when you use one. Once decided, it stops being a guess — run the **Design decision loop** above to make it official.
