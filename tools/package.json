{
  "name": "fakeamon-tools",
  "private": true,
  "type": "module",
  "description": "Developer-only helper scripts (never loaded by the game). The game itself stays plain HTML/JS with no build step — see CLAUDE.md.",
  "scripts": {
    "slice": "node slice-sheets.mjs"
  },
  "dependencies": {
    "sharp": "^0.35.3"
  }
}
